## This file contains code to train the VAE directly; inputs of history will be fed in, and the outputs will be the policy
## To train this directly, need a dataset of (history, policy) pairs; if not, then need to train with PPO.
